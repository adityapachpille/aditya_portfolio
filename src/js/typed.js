import Typed from 'typed.js';

var options = {
  strings: ["A Front-End Developer!", "A Wordpress Developer!"],
  typeSpeed: 120,
  backSpeed: 50,
  backDelay: 1500,
  loop: true
};

var typed = new Typed('#headline', options);
